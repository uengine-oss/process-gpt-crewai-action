import time
import json
import logging
import asyncio
from typing import Dict, List, Optional, Any, Tuple
from processgpt_agent_utils.tools.dmn_rule_tool import DMNRuleTool
from processgpt_agent_utils.tools.knowledge_manager import Mem0Tool

# 로깅 설정
logger = logging.getLogger(__name__)


class DynamicPromptGenerator:
    """동적 프롬프트 생성기 주어진 입력들을 바탕으로 Task용 description과 expected_output을 생성합니다."""

    def __init__(self, llm):
        self.llm = llm


    async def generate_task_prompt(
        self,
        task_instructions: str,
        agent_info: List[Dict],
        form_types: Dict = None,
        form_html: str = "",
        feedback_summary: str = "",
        current_activity_name: str = "",
        user_info: Optional[List[Dict]] = None,
        sources: List[Dict] | None = None,
    ) -> Tuple[str, str]:
        """두 LLM 호출로 설명/결과물을 분리 생성하고 asyncio.gather로 병렬 실행."""

        learned_knowledge = self._collect_learned_knowledge(
            agent_info=agent_info,
            task_instructions=task_instructions,
            feedback_summary=feedback_summary,
        )

        dmn_analysis = self._collect_dmn_analysis(
            agent_info=agent_info,
            task_instructions=task_instructions,
        )

        # 설명용 브리프: 폼 정보 제외
        desc_brief = self._build_description_prompt(
            task_instructions=task_instructions,
            agent_info=agent_info,
            user_info=user_info or [],
            feedback_summary=feedback_summary,
            current_activity_name=current_activity_name,
            learned_knowledge=learned_knowledge,
            dmn_analysis=dmn_analysis,
            form_types=form_types,
            sources=sources,
        )

        # 결과물용 브리프: 폼 정보만 포함
        expected_brief = self._build_expected_output_prompt(
            form_types=form_types,
            form_html=form_html,
        )

        description_system = self._build_system_prompt_description()
        expected_system = self._build_system_prompt_expected_output()

        async def ainvoke_text(system_prompt: str, user_prompt: str) -> str:
            """LLM을 호출해 순수 텍스트 응답을 받아요"""
            
            start_time = time.time()
            response = await self.llm.ainvoke([
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_prompt},
            ])
            raw = getattr(response, "content", response)
            text = "".join(part.get("text", "") if isinstance(part, dict) else str(part) for part in raw) if isinstance(raw, list) else str(raw)
            text = (text or "").strip()
            logger.info("📝 LLM(ainvoke) 완료 - %.2fs, %d chars", time.time() - start_time, len(text))
            if not text:
                logger.error("❌ LLM 빈 응답 수신: system_prompt 길이=%d, user_prompt 길이=%d", len(system_prompt), len(user_prompt), exc_info=False)
                raise ValueError("Empty response from LLM")
            return text

        desc_task = asyncio.create_task(ainvoke_text(description_system, f"[ROLE=description]\n{desc_brief}\n설명만 한 문단으로."))
        out_task  = asyncio.create_task(ainvoke_text(expected_system, f"[ROLE=expected_output]\n{expected_brief}"))
        
        try:
            description, expected_output = await asyncio.gather(desc_task, out_task)
            logger.info("✅ 비동기 분리 프롬프트 생성 완료")
            return description, expected_output
        except Exception as e:
            logger.error("❌ 동적 Task description/expected_output 생성 실패(raise): %s", e, exc_info=True)
            raise

    # ----------------------------
    # 내부 로직
    # ----------------------------
    def _collect_learned_knowledge(
        self,
        agent_info: List[Dict],
        task_instructions: str,
        feedback_summary: str,
    ) -> Dict[str, str]:
        """에이전트별 관련 학습 내용 수집"""
        if not task_instructions or not task_instructions.strip():
            return {}

        query = f"{task_instructions.strip()}\n{feedback_summary.strip()}"
        logger.info("🧠 mem0 사전 훈련 데이터 검색 시작")

        learned: Dict[str, str] = {}
        for ag in agent_info:
            agent_id = ag.get("id")
            tenant_id = ag.get("tenant_id")
            role = ag.get("role", "Unknown")

            if not (agent_id and tenant_id):
                continue

            try:
                mem0_tool = Mem0Tool(tenant_id=tenant_id, user_id=agent_id)
                result = mem0_tool._run(query)
                if result and "지식이 없습니다" not in result:
                    learned[role] = result
            except Exception as e:
                logger.warning("⚠️ 에이전트 %s 메모리 검색 실패: %s", role, e)

        return learned


    def _build_description_prompt(
        self,
        task_instructions: str,
        agent_info: List[Dict],
        user_info: List[Dict],
        feedback_summary: str,
        current_activity_name: str,
        learned_knowledge: Dict[str, Any],
        dmn_analysis: Dict[str, str],
        form_types: Optional[Dict] = None,
        sources: List[Dict] | None = None,
    ) -> str:
        """설명 프롬프트: form_types/form_html 제외, 나머지 컨텍스트를 원문 스타일로 포함."""
        
        has_feedback = bool(feedback_summary and feedback_summary.strip() and feedback_summary.strip() != '없음')
        has_learned = bool(learned_knowledge and any(str(v).strip() for v in learned_knowledge.values()))
        has_dmn = bool(dmn_analysis and any(str(v).strip() for v in dmn_analysis.values()))
        agent_info_json = json.dumps(agent_info or [], ensure_ascii=False, indent=2) if agent_info else '정보 없음'
        user_info_json = json.dumps(user_info or [], ensure_ascii=False, indent=2) if user_info else '정보 없음'
        learned_json = json.dumps(learned_knowledge or {}, ensure_ascii=False, indent=2) if has_learned else '관련 경험 없음'
        dmn_json = json.dumps(dmn_analysis or {}, ensure_ascii=False, indent=2) if has_dmn else '관련 규칙 분석 없음'
        sources_json = json.dumps(sources or [], ensure_ascii=False, indent=2) if sources else '소스 파일 없음'
        # agent_info에서 실제 agent_id와 tenant_id 추출
        agent_context_info = []
        if agent_info:
            for ag in agent_info:
                agent_id = ag.get("id") or ag.get("user_id")
                tenant_id = ag.get("tenant_id")
                if agent_id and tenant_id:
                    agent_context_info.append(f"agent_id={agent_id}, tenant_id={tenant_id}")
        
        agent_context_text = "\n".join([f"- {info}" for info in agent_context_info]) if agent_context_info else "없음"
        
        # form_types에서 필드 타입 확인
        has_slide_type = False
        has_report_type = False
        if form_types:
            form_fields = None
            if isinstance(form_types, dict) and ("fields" in form_types or "html" in form_types):
                form_fields = form_types.get("fields")
            else:
                form_fields = form_types if form_types else None
            
            if form_fields and isinstance(form_fields, list):
                for field in form_fields:
                    if isinstance(field, dict):
                        field_type = field.get("type", "").lower()
                        if field_type in ["slide", "presentation"]:
                            has_slide_type = True
                        elif field_type in ["report", "document"]:
                            has_report_type = True
        
        # 슬라이드와 리포트 형식 섹션 내용 생성
        slide_section = """**슬라이드 형식 (presentation, slide 등):**
- **구조**: 제목 슬라이드 → 목차 → 본문 슬라이드들 → 결론/질의응답 슬라이드
- **분량**: 최소 10장 이상 구성(많을 수록 좋음)
- **형식**: reveal.js 마크다운 형식으로 결과물 생성
- **내용 기반**: 보고서가 있을 경우 보고서 내용을 기반으로 슬라이드 생성, 없으면 주제에 맞게 적절히 생성
- **기술적 요구사항**:
  * reveal.js 구문 사용 (새 슬라이드용 ---, 수직 슬라이드용 --)
  * 불릿 포인트, 헤더, 강조 형식 적절히 활용
  * 논리적 슬라이드 전환과 흐름
  * 깔끔하고 전문적인 프레젠테이션 구조
  * 적절한 경우 사용자 정보 통합(발표자 이름, 부서 등)
- **🚨 중요한 출력 형식 규칙**:
  * 절대로 ```markdown, ```html, ``` 같은 코드 블록으로 결과를 감싸지 마세요
  * 마크다운 내용을 출력하세요 (코드 블록 없이)
  * reveal.js 마크다운 구문을 그대로 사용하되, 코드 블록으로 감싸지 말 것
  * HTML 주석이나 코드 블록 형태의 감싸기는 절대 금지

""" if has_slide_type else ""
        
        report_section = """**리포트 형식 (report, document 등):**
- **형식**: 마크다운(Markdown) 형식으로 결과물 생성
- **기본 구조**: 서론 → 본론 → 결론 형식
- **소제목과 목차**: 내용에 맞게 유연하게 적절히 알아서 생성
- **내용 작성 원칙**:
  * 데이터 기반 객관적 서술
  * 논리적 흐름과 근거 제시
  * 내용이 많을수록 좋음 (상세하고 풍부한 내용 작성)
- **출처 표기**: 내용을 검색이나 참고 자료에서 가져왔다면 반드시 출처 명시
  * 마크다운 링크 형식: `[출처명](URL)` 또는 각주 형식 사용
  * 참고문헌 섹션에 모든 출처 정리

""" if has_report_type else ""

        if has_feedback:
            first_priority_text = """🔥 1순위 - 피드백 절대 우선:
   - 피드백 요구사항이 모든 지시사항보다 우선 (작업지시사항, 학습경험 등 모두 피드백에 종속)
   - 피드백에서 요구한 변경사항을 정확히 이해하고 100% 적용
   - 기존 방식/관례를 완전히 버리고 피드백이 제시한 새로운 방식으로 전환
   - 피드백 vs 다른 지시사항 충돌 시 → 무조건 피드백 우선
   
   🔄 피드백 동사별 작업지시사항 재해석 규칙:
   - 피드백 "저장" + 작업지시사항 "수정" → INSERT 작업으로 처리
   - 피드백 "수정" + 작업지시사항 "저장" → UPDATE 작업으로 처리
   - 피드백 "삭제" + 작업지시사항 "저장" → DELETE 작업으로 처리
   - 피드백 "조회" + 작업지시사항 "저장" → SELECT 작업으로 처리
   - 여러가지 동사가 있으면 피드백의 동사가 실제 수행할 작업 유형을 최종 결정
   - 피드백의 동사가 실제 수행할 작업 유형을 최종 결정"""
        else:
            first_priority_text = "1순위 - 작업지시사항 그대로 수행"

        if has_learned and has_feedback:
            second_priority_text = """2순위 - DMN 규칙 우선 활용 및 학습된 경험 참고:
   - 피드백 범위 내에서 DMN 규칙을 최우선으로 활용하여 규칙 기반 추론 수행하고, 학습된 경험을 보조적으로 참고하여 더 완벽하게 처리
   - DMN 규칙이 있으면 반드시 그 규칙을 기반으로 모든 추론과 결정을 수행하고, 피드백이 요구하는 방향성을 유지하면서 경험으로 디테일 보완"""
        elif has_learned:
            second_priority_text = """2순위 - DMN 규칙 우선 활용 및 학습된 경험 참고:
   - 작업지시사항은 그대로 하되, DMN 규칙을 최우선으로 활용하여 규칙 기반 추론 수행하고, 학습된 경험을 보조적으로 참고해서 더 디테일하고 완벽하게 처리
   - DMN 규칙이 있으면 반드시 그 규칙을 기반으로 모든 추론과 결정을 수행하고, 경험에서 얻은 노하우로 품질과 정확성 향상"""
        else:
            second_priority_text = """2순위 - DMN 규칙 우선 활용 및 일반 배경지식 참고:
   - DMN 규칙을 최우선으로 활용하여 규칙 기반 추론 수행하고, 일반 배경지식을 보조적으로 참고
   - DMN 규칙이 있으면 반드시 그 규칙을 기반으로 모든 추론과 결정을 수행"""

        return f"""
다음 정보를 바탕으로 CrewAI Task description 프롬프트를 생성하세요:

=== 📋 섹션 1: 입력값 설명 ===

**활동명 (current_activity_name):**
- 값: {current_activity_name or '일반 작업'}
- 역할: 현재 수행중인 업무 이름을 나타냄
- 활용: 이름이 의미하는 작업의 배경과 목적 설명에 사용

**팀 구성 (agent_info):**
- 값: {agent_info_json}
- 역할: 협업할 에이전트들의 역할, ID, 테넌트 정보 제공
- 활용: 각 에이전트의 전문성을 고려한 작업 분배 및 협업 지시에 사용

**담당자(Owner) (user_info):**
- 값: {user_info_json}
- 역할: 현재 업무의 실제 담당자 정보(담당자 표기, 연락/검토 지점 반영)
- 활용: 결과물에 담당자/참가자 정보가 요구되면 적절히 반영

**작업 지시사항 (task_instructions):**
- 값: {task_instructions or '명시되지 않음'}
- 역할: 기본적으로 수행해야 할 핵심 업무 내용과 지침 및 이전 결과물들에 대한 정리본
- 활용: Task의 주요 목표와 수행 방법의 기준점 (단, 피드백이 있으면 피드백에 의해 재해석됨)

**학습된 경험 (learned_knowledge):**
- 값: {learned_json}
- 역할: 에이전트별 관련 업무 경험과 노하우 제공
- 활용: 작업 품질 향상과 실수 방지를 위한 참고 자료
{('- 참고: 현재 수행할 작업을 더 완벽하게 수행하기 위한 디테일 보완에 활용' if has_learned else '- 참고: 학습 자료 부족 시에도 작업 중단 금지, 일반 지식으로 초안 작성')}

**DMN 규칙 분석 (dmn_rule):**
- 값: {dmn_json}
- 역할: 규칙을 기반으로 추론된 답변 제공(학습된 경험보다 우선 반영)
- 활용: 작업지시사항 해석 및 원자 작업 도출 시 규칙으로 추론된 결과를 최우선으로 반영

**피드백 (feedback_summary):**
- 값: {feedback_summary if has_feedback else '없음'}
- 역할: 이전 작업에 대한 수정 요구사항 (최고 우선순위)
- 활용: 모든 다른 지시사항보다 우선하여 작업 방향과 방법을 결정
{f'- 🔥 최우선: 피드백이 있으면 모든 작업은 이 피드백 내용에 따라 재정의됨' if has_feedback else ''}
- 처리방식: 피드백 동사(저장/수정/삭제/조회 등..)가 있으면 그에 맞게 작업지시사항 재해석
- 충돌해결: 피드백 vs 작업지시사항 충돌 시 → 무조건 피드백 우선 적용

**소스 파일 (sources):**
- 값: {sources_json}
- 역할: 작업 수행에 필요한 소스 파일 정보 제공
- 활용: 소스 파일을 참고하여 작업 수행, file_path 필드를 통해 파일 경로에 접근하여 파일 내용을 참고

=== 🎯 섹션 2: 작업 범위 및 방향 ===

**우선순위 체계:**
{first_priority_text}

{second_priority_text}

**작업 범위 제한 원칙:**
- 오로지지 작업 지시사항과 피드백만의 작업 방향을 결정
- 명시된 작업만 수행, 비명시 연관작업/후속작업 절대 금지
- 예시 1: "휴가 정보 저장" → 오직 휴가정보만 저장, 휴가잔여일수 수정/알림발송/승인처리 등 금지
- 예시 2: "주문 정보 저장" → 오직 주문정보만 저장, 재고감소/포인트적립/알림발송 등 금지
- 예시 3: "사용자 정보 수정" → 명시된 사용자의 명시된 필드만 수정, 다른 사용자/필드 수정 금지

**다중 작업 처리 원칙:**
- 반드시 작업지시사항에서 실행 동사를 기준으로 원자 작업을 모두 추출하여 목록화(예: 저장, 확인, 조회 등)
- 모든 원자 작업을 반드시 수행해야 함 
- 즉, 작업지시에 여러 작업이 있을 경우 해당 작업을 모두 수행해야 함
- 작업 간 선후관계/의존성을 파악하여 올바른 순서로 실행

=== 💾 섹션 3: 데이터 쓰기/수정 시 정확성 보장 ===

**데이터 완전성 확보 절차:**
1. 작업 지시사항에서 필요한 모든 데이터 파악
2. 부족한 데이터는 읽기전용 도구로 조회/검증/보완 (SELECT 쿼리, 검색 API, 메시징 API 등)
3. 모든 필요 데이터가 완전해진 후에만 쓰기 작업 수행
4. 데이터를 저장 및 수정할 경우, 주어진 값을 최대한 활용해서 다른 테이블의 값을 조회하는 등, 툴을 적극 활용해서 완전한 데이터를 생성 및 수정해야 함, 절대 누락되는 컬럼이나 데이터가 있어서는 안됩니다.

=== 🎨 섹션 4: 콘텐츠 생성 시 가이드라인 ===

**폼 형식별 창의적 콘텐츠 생성 원칙:**

{slide_section}{report_section}**일반 폼 데이터 생성:**
- 폼 타입의 이름과 용도에 맞는 적절한 내용 생성
- 폼 타입의 모든 필수 필드 완전히 채움
- 실제 업무에서 사용할 수 있는 현실적인 데이터 생성
- 일관성 있는 데이터 관계 유지 (예: 부서-직책-권한 매칭)
- 폼의 목적과 맥락에 부합하는 의미 있는 데이터 생성

**파일 생성 작업 필수 조건:**
- 작업 지시사항(task_instructions)을 분석하여 파일 생성 요청인지 판단하세요
- 파일 생성 요청(예: 문서 작성, 리포트 생성, 프레젠테이션 제작, 이미지 생성, 데이터 파일 생성 등)이 포함된 경우, 반드시 다음을 수행해야 합니다:
  * 다운로드 가능한 URL을 제공할 수 있는 도구나 스킬을 활용하여 결과물을 제공
  * 생성된 파일을 Supabase 또는 기타 스토리지에 업로드
  * 업로드된 파일의 다운로드 가능한 URL을 획득
  * URL의 유효성 및 접근 권한을 검증
  * 폼_데이터에 다운로드 URL 필드를 포함 (예: download_url, file_download_url 등 명확한 키 사용)
- 해당 도구/스킬이 없다면 먼저 적합한 도구 사용 가능 여부를 확인하고, 사용 불가 시 명확한 사유와 함께 실패 상태로 보고
- 생성된 파일은 사용자나 후속 시스템이 즉시 접근 가능한 형태로 제공되어야 합니다

**작업 지시사항 분석 및 구체화 원칙:**
- 작업 지시사항(task_instructions)을 먼저 분석하여 요청의 성격과 구체성을 판단하세요
- 작업 지시사항이 모호하거나 단순한 요청 형태(예: "PPT 만들어줘", "보고서 작성해줘" 등)인 경우, 실제 수행 가능한 구체적인 단계로 세분화하여 Task description을 작성하세요
- 단순 지시를 그대로 재진술하지 말고, 다음을 포함한 실행 가능한 상세 액션 플랜을 작성하세요:
  * 파일 생성, 실행, 업로드, URL 제공 등 필수 절차의 명확한 정의
  * 사용할 도구와 스킬의 구체적 지정
  * Supabase 및 기타 스토리지/전송 도구를 활용한 산출물 업로드 절차
  * 검증된 다운로드 URL 제공을 위한 단계별 지침
  * 각 단계별 검증 절차
- 작업 지시사항이 이미 구체적이고 상세한 경우에는 그대로 반영하되, 누락된 실행 단계가 있다면 보완하세요

**도구 활용 기본 원칙:**
- 제공된 도구와 스킬은 가능한 한 광범위하게 모두 활용하여 정보 수집과 작업 수행 정확도를 높일 것
- 실행형 도구가 의존성·환경 오류를 보고하면 즉시 'run_shell' 등 환경 설정 도구로 필요한 패키지 설치·설정을 수행하고, 동일 작업을 재시도하여 성공 여부를 확인
- 특정 도구 사용이 실패하거나 제한되는 경우, 동일 목표를 달성할 수 있는 다른 도구/스킬을 즉시 탐색하여 대체 실행
- 대체 도구 사용 시에도 결과 데이터의 완전성과 일관성을 유지하며, 필요 시 복수 도구를 연계 사용
- **🚨 도구 실패 시 처리 (핵심 규칙):**
  * 도구는 보조 수단일 뿐, 도구 실패 ≠ 작업 실패
  * 파일이 없어도, URL이 깨져도, 도구가 오류를 반환해도 → 입력 텍스트(파일 내용용,작업지시사항, InputData 등)가 있으면 그것으로 작업 계속
  * 모든 도구(perplexity, supabase, gmail-mcp, mcp-python-interpreter, mem0, memento, dmn_rule 등)가 실패해도 → 입력 컨텍스트 + 배경지식으로 작업 완료
  * list_directory "No files found", dmn_rule "규칙 없음", mem0 "지식 없음" 등 빈 결과도 실패 사유 아님
  * 오직 입력 컨텍스트(텍스트/데이터)가 전혀 없을 때만 FAILED 처리하고 부족한 정보 명시
- **도구 파라미터 사용 규칙:**
  * 도구 호출 시 agent_id나 tenant_id 파라미터가 필요한 경우, 반드시 아래의 실제 값을 사용해야 함
  * 절대로 임의의 값이나 동적으로 생성한 값을 사용하지 말 것
  * 실제 agent_id와 tenant_id 값:
{agent_context_text}

**콘텐츠 생성 시 주의사항:**
- 모든 가용 도구를 적극 활용하여 정보 수집하되, 도구 실패 시에도 작업 계속:
  * human_asked 툴은 반드시 딱 각 질문당 한번만 사용하며, 중복 호출 금지 오로직 보안관련 이슈가 있을 때만 사용하세요
  * 1단계: 입력 컨텍스트(rfp_file_content, task_instructions, InputData 등) 확인 및 추출
  * 2단계: 가용 도구들(perplexity, supabase, gmail-mcp, mcp-python-interpreter, mem0, memento, dmn_rule 등)로 추가 정보 수집 시도
  * 3단계: 도구 결과가 실패/없음/오류여도 → 1단계 컨텍스트 + 배경지식으로 작성 계속
  * 🚨 핵심: 도구는 선택, 컨텍스트는 필수 → 도구 없이도 컨텍스트만으로 완료 가능
  * 메모리 도구(mem0, memento) 결과 없음 → 작업 중단 금지, 일반 지식 활용
  * DB 조회(supabase) 실패/빈 결과 → 작업지시사항 데이터 활용
  * 파일 도구(list_directory) "No files" → 입력 텍스트 데이터 활용
  * 파일 생성 작업 시 upload_file 등으로 URL 제공, 실패 시에도 내용은 반드시 작성
  * 폼 요구사항과 작업 맥락에 맞는 적절한 내용 생성 (도구 성공 여부 무관)

=== 📤 프롬프트 생성 최종 지침 ===

**달성 목표 (작업지시사항 범위 내에서만):**
- 구체적으로 무엇을 완성해야 하는지
- 어떤 품질과 기준을 만족해야 하는지
- 특별히 주의해야 할 요구사항이 있는지
- 범위를 벗어난 작업은 절대 수행하지 않을 것
- 데이터 처리 시 완전성과 정확성을 보장할 것

**성공 기준:**
- 명시된 목표들이 전부 달성되어야 함 (일부만 달성하면 실패이며, 실패 시 반드시 사유를 디테일하게 명시)
{('- 피드백 내용을 100% 반영하여 처리' if has_feedback else '')}
- 요구된 형식으로 결과 제공
- 작업 범위 엄수 확인
"""

    def _build_expected_output_prompt(
        self,
        form_types: Optional[Dict],
        form_html: str,
    ) -> str:
        """결과물 프롬프트: form_types/form_html만 포함(원문 폼 섹션 + 선택형 규칙)."""
        
        has_form_types = bool(form_types)
        form_fields = None
        form_html_text = form_html or ""
        if isinstance(form_types, dict) and ("fields" in form_types or "html" in form_types):
            form_fields = form_types.get("fields")
            html = form_types.get("html")
            if html and isinstance(html, str) and html.strip():
                form_html_text = html
        else:
            form_fields = form_types if form_types else None
        is_multidata_mode = bool(form_html_text and 'is_multidata_mode="true"' in form_html_text)

        # 리포트/슬라이드 타입 필드 감지
        report_slide_fields = []
        if form_fields and isinstance(form_fields, list):
            for field in form_fields:
                if isinstance(field, dict):
                    field_type = field.get("type", "").lower()
                    field_key = field.get("key", "")
                    if field_type in ["report", "document", "slide", "presentation"] and field_key:
                        report_slide_fields.append({
                            "key": field_key,
                            "type": field_type
                        })

        form_fields_json = json.dumps(form_fields, ensure_ascii=False, indent=2) if form_fields else '특별한 형식 제약 없음'
        multidata_notice = (
            '- 🚨 다중 데이터 모드: is_multidata_mode="true" 속성이 있으면 해당 필드는 배열 형태로 반환해야 함\n\n'
            if is_multidata_mode else ''
        )
        
        # 리포트/슬라이드 타입 필드 처리 지침 생성
        report_slide_notice = ""
        if report_slide_fields:
            field_names = [f['key'] for f in report_slide_fields]
            report_slide_notice = (
                f"\n"
                f"섹션 8) 리포트/슬라이드 타입 필드 처리 (🚨 필수)\n"
                f"- 다음 필드들은 리포트 또는 슬라이드 타입입니다: {', '.join(field_names)}\n"
                f"- 이 필드들의 마크다운 내용은 폼_데이터에 포함하지 말고, 별도의 JSON 객체로 반환해야 합니다\n"
                f"- 각 필드의 키 이름(예: 'budget_report', 'budget_slide')을 그대로 사용하여 별도 JSON 생성\n"
                f"- 반환 형식: 각 필드마다 독립적인 JSON 객체로 반환\n"
                f"  예시 1 (리포트 필드가 있는 경우):\n"
                f"  {{\"budget_report\": \"# 리포트 제목\\n\\n리포트 마크다운 내용...\"}}\n"
                f"  예시 2 (슬라이드 필드가 있는 경우):\n"
                f"  {{\"budget_slide\": \"---\\n\\n# 슬라이드 제목\\n\\n슬라이드 마크다운 내용...\"}}\n"
                f"  예시 3 (리포트와 슬라이드 둘 다 있는 경우):\n"
                f"  {{\"budget_report\": \"# 리포트 제목\\n\\n리포트 마크다운 내용...\"}}\n"
                f"  {{\"budget_slide\": \"---\\n\\n# 슬라이드 제목\\n\\n슬라이드 마크다운 내용...\"}}\n"
                f"- 🚨 중요 규칙:\n"
                f"  * 리포트/슬라이드 타입 필드는 폼_데이터에 포함하지 않습니다\n"
                f"  * 각 필드의 마크다운 내용을 해당 필드의 키 이름으로 별도 JSON 객체로 반환합니다\n"
                f"  * 키 이름은 폼 필드의 'key' 값을 그대로 사용합니다 (예: 'budget_report', 'budget_slide')\n"
                f"  * 마크다운 내용은 코드 블록(```)으로 감싸지 말고 순수 마크다운 텍스트로 반환합니다\n"
                f"  * 여러 개의 리포트/슬라이드 필드가 있으면 각각 별도 JSON 객체로 반환합니다\n"
                f"  * ⚠️ 시스템 참고: 이 필드들은 시스템에서 자동으로 별도 이벤트(crew_type: 'report' 또는 'slide')로 발행됩니다\n"
                f"    따라서 반드시 폼_데이터와 분리하여 최상위 레벨에 별도 키로 반환해야 합니다\n\n"
            )

        return (
            "다음 정보를 바탕으로 CrewAI Task expected_output 프롬프트를 생성하세요:\n\n"
            "=== 📋 폼 섹션 (expected_output 전용) ===\n\n"
            "섹션 1) 폼 형식(form_types)\n"
            f"- 값(필드 정의): {form_fields_json}\n"
            f"- 값(HTML): {form_html_text if form_html_text else '없음'}\n"
            "- 역할: 최종 결과물의 구조와 필드 정의, 선택형 항목(items) 제공\n"
            "- 활용: expected_output 구조 설계와 폼_데이터 키/값 결정에 사용\n"
            "- 🚨🚨🚨 절대 규칙: 폼_데이터의 키는 반드시 form_fields의 'key' 값을 사용 (절대 'text' 값 사용 금지!)\n"
            "- 예시: form_fields에 {\"key\": \"inquiry_type\", \"text\": \"문의 유형\"}이 있다면\n"
            "  ✅ 올바름: {\"폼_데이터\": {\"inquiry_type\": \"값\"}}\n"
            "  ❌ 절대 금지: {\"폼_데이터\": {\"문의 유형\": \"값\"}} (text 값 사용)\n"
            "  ❌ 절대 금지: {\"폼_데이터\": {\"문의유형\": \"값\"}} (임의 생성)\n"
            "  ❌ 절대 금지: {\"폼_데이터\": {\"CS_내용\": \"값\", \"분류_결과\": \"값\"}} (form_fields에 없는 키)\n"
            f"{multidata_notice}"
            "섹션 2) 선택형(radio/select) 처리\n"
            "- form_fields의 type이 'radio' 또는 'select'인 경우, 값 목록은 form_types의 HTML에서 추출\n"
            "- 예: <radio-field name=\"review_result\" items=\"[{{{'approve':'승인'}}},{{{'reject':'반려'}}}]\" ...>\n"
            "- 파싱 규칙:\n"
            "  1) HTML의 items 속성 문자열을 JSON으로 변환(단일따옴표 → 쌍따옴표) 후 파싱\n"
            "  2) 항목 객체의 key가 실제 저장 값, value는 한글 라벨\n"
            "  3) 최종 폼_데이터에는 key 값 사용 (예: 'approve' 또는 'reject')\n"
            "  4) radio/select 외 필드는 type에 맞는 적절한 텍스트/숫자 형식 사용\n"
            "  5) user-select-field는 담당자(Owner)의 식별자 사용 (id 우선, 없으면 email)\n\n"
            "섹션 3) expected_output 상세 작성 지침\n"
            "- 반드시 JSON 객체(object)로만 작성하세요. 문자열 포장/백틱/코드블록 금지\n"
            "- 아래 예시는 구조 예시이며, 값은 반드시 실제 작업 결과로 채워야 합니다\n\n"
            "[일반 모드 예시 (is_multidata_mode=\"false\" 또는 없음)]\n"
            "{\n"
            '  "상태": "SUCCESS",\n'
            '  "수행한_작업": "입력 데이터의 [핵심 속성]을 분석했습니다. [구체적 판단 기준]에 따라 [최종 결과값]으로 결정했으며, 그 이유는 [상세한 근거 설명]입니다. [추가 참고 정보나 사용한 도구]를 활용했습니다.",\n'
            '  "폼_데이터": {\n'
            '    "field_key_1": "실제 데이터 값",  // form_fields의 첫 번째 필드 key\n'
            '    "field_key_2": "실제 데이터 값"   // form_fields의 두 번째 필드 key\n'
            "  }\n"
            "}\n"
            "🚨 중요: \n"
            "- field_key_1, field_key_2는 예시입니다. 반드시 위 form_fields의 실제 'key' 값으로 교체하세요!\n"
            "- 모든 판단 근거와 이유는 \"수행한_작업\"에 포함하세요 (절대 \"분류_근거\", \"판단_이유\" 등 별도 키 생성 금지)\n"
            "- 최상위 키는 \"상태\", \"수행한_작업\", \"폼_데이터\" 3개만 사용하세요\n\n"
            "[다중 데이터 모드 예시 (is_multidata_mode=\"true\")]\n"
            "{\n"
            '  "상태": "SUCCESS",\n'
            '  "수행한_작업": "입력 데이터의 [핵심 속성]을 분석했습니다. [구체적 판단 기준]에 따라 [최종 결과값]으로 결정했으며, 그 이유는 [상세한 근거 설명]입니다. [추가 참고 정보나 사용한 도구]를 활용했습니다.",\n'
            '  "폼_데이터": {\n'
            '    "normal_field_key": "실제 데이터 값",  // form_fields의 일반 필드 key\n'
            '    "multidata_field_key": [  // form_fields의 다중 데이터 필드 key\n'
            "      {\n"
            '        "property_key_1": "실제 속성 값",  // HTML name 속성의 실제 key\n'
            '        "property_key_2": "실제 속성 값"\n'
            "      },\n"
            "      {\n"
            '        "property_key_1": "실제 속성 값2",\n'
            '        "property_key_2": "실제 속성 값2"\n'
            "      }\n"
            "    ]\n"
            "  }\n"
            "}\n"
            "🚨 중요: \n"
            "- normal_field_key, multidata_field_key, property_key_1 등은 모두 예시입니다. 반드시 form_fields의 실제 'key' 값으로 교체하세요!\n"
            "- 모든 판단 근거와 이유는 \"수행한_작업\"에 포함하세요 (절대 \"분류_근거\", \"판단_이유\" 등 별도 키 생성 금지)\n"
            "- 최상위 키는 \"상태\", \"수행한_작업\", \"폼_데이터\" 3개만 사용하세요\n\n"
            "- 위 구조는 예시입니다. 예시 값을 그대로 사용하지 말고 실제 결과로 대체하세요\n"
            "- 🚨🚨🚨 폼_데이터 키 규칙 (최우선 규칙):\n"
            "  * 반드시 form_fields의 'key' 값을 변경 없이 원본 그대로 정확히 필드명으로 사용\n"
            "  * 절대 'text' 값을 키로 사용하지 말 것 (예: \"문의 유형\", \"검토 결과\" 등 한글 키 금지)\n"
            "  * 🔥 절대 임의의 키를 생성하지 말 것 (예: \"CS_내용\", \"분류_결과\", \"분류_근거\", \"판단_이유\" 등 form_fields에 없는 키 절대 금지)\n"
            "  * form_fields에 정의된 모든 필드가 폼_데이터에 정확히 포함되어야 함\n"
            "  * 🔥 근거나 이유는 \"수행한_작업\"에 포함하고, 절대 별도 키로 분리하지 말 것\n"
            "- '수행한_작업'은 가독성 좋은 자연어로 상세하게 작성 (불릿/번호 허용):\n"
            "  * 🚨 최우선: 왜 그런 결과/판단을 내렸는지 이유와 근거를 중점적으로 설명\n"
            "  * 어떤 입력 데이터나 정보를 분석했는지\n"
            "  * 어떤 기준이나 패턴으로 판단했는지\n"
            "  * 어떤 프로세스나 도구를 사용했는지\n"
            "  * 최종 결과가 무엇인지\n"
            "  예시: \"입력 데이터의 [특정 속성/패턴]을 분석한 결과, [판단 기준]에 해당하여 [결과값]으로 결정했습니다. [추가 근거나 참고 정보]를 고려했습니다.\"\n"
            "- 작업지시사항에서 추출한 모든 원자 작업을 입력 컨텍스트 범위 내에서 수행 (입력 컨텍스트 없으면 FAILED)\n"
            "- 다중 데이터 모드: is_multidata_mode=\"true\" 필드는 반드시 배열 형태로 반환(HTML의 실제 name 사용)\n"
            "- 🚨 도구 실패, 파일 없음, 빈 결과 등은 작업 실패 사유가 아님 (입력 컨텍스트만 있으면 SUCCESS)\n\n"
            "섹션 4) 선택형(radio/select) 값 결정 지침\n"
            "- form_types에 HTML이 제공되면 해당 필드의 items를 HTML에서 파싱하여 실제 선택 가능한 값 목록 결정\n"
            "- items 예시: [{\"approve\":\"승인\"},{\"reject\":\"반려\"}] → 폼_데이터 값은 \"approve\" 또는 \"reject\" 중 하나여야 함\n\n"
            "섹션 5) 다중 데이터 모드 필드 처리 지침\n"
            "- HTML에서 is_multidata_mode=\"true\" 속성이 있는 필드는 반드시 배열 형태로 반환\n"
            "- 배열 내 각 객체는 해당 섹션의 모든 필드를 포함해야 함\n"
            "- 다중 데이터 모드 필드의 각 배열 요소는 독립적인 완전한 데이터 객체여야 함\n"
            "- 필드명은 HTML의 name 속성을 그대로 사용하고, 해당 섹션의 모든 하위 필드들을 포함하여 배열 요소로 구성\n"
            "- 🚨 중요: multidata_field는 실제 HTML의 name 속성을 활용하세요. 임의로 생성하지 말고 HTML을 그대로 적극 활용하여 구조를 구성\n\n"
            "섹션 6) 실패(FAILED) 판단 기준 (🚨 핵심 규칙)\n"
            "- FAILED는 오직 입력 컨텍스트(텍스트/데이터)가 전혀 없을 때만 사용\n"
            "- 파일 없음, URL 깨짐, 도구 실패, 검색 결과 없음 등은 FAILED 사유가 아님\n"
            "- 입력 데이터(rfp_file_content, task_instructions 내 텍스트 등)가 있으면 반드시 SUCCESS로 완료\n"
            "- 도구(perplexity, supabase, gmail-mcp, mcp-python-interpreter, mem0, memento, dmn_rule 등) 전부 실패해도 입력 컨텍스트 있으면 SUCCESS\n"
            "- FAILED 사용 시 반드시 '어떤 정보가 부족한지' 구체적으로 명시\n\n"
            "섹션 7) 응답 형식 (🚨 필수 규칙)\n"
            "- 오직 expected_output 객체(JSON)만 응답하세요. 다른 텍스트 포함 금지\n"
            "- 🚨 필수 최상위 키: \"상태\", \"수행한_작업\", \"폼_데이터\" (이 3개는 반드시 포함)\n"
            "- 🚨 폼_데이터 내부 키: 반드시 form_fields의 'key' 값만 사용 ('text' 값 절대 사용 금지)\n"
            "- 🚨 수행한_작업 작성 규칙:\n"
            "  * 🔥 최우선: 왜 그런 결과/판단을 내렸는지 이유와 근거를 가장 중점적으로 설명\n"
            "  * 어떤 입력 데이터를 분석했는지\n"
            "  * 어떤 기준으로 판단했는지\n"
            "  * 어떤 프로세스나 도구를 사용했는지\n"
            "  * 2-4문장으로 상세하게 작성 (단순히 \"확인했습니다\" 같은 짧은 문장 금지)\n"
            "✅ 올바른 예시 구조:\n"
            "{\n"
            '  "상태": "SUCCESS or FAILED",  // 최상위 키 1\n'
            '  "수행한_작업": "입력 데이터의 [핵심 속성/패턴]을 분석한 결과, [구체적 판단 기준]에 해당하여 [결과값]으로 결정했습니다. 이는 [상세한 근거와 이유]이기 때문입니다. [추가 고려사항이나 참고 정보]를 활용했습니다.",  // 최상위 키 2 - 모든 근거와 이유를 여기에 포함\n'
            '  "폼_데이터": {  // 최상위 키 3 - form_fields의 key만 사용\n'
            '    "form_field_key_1": "값1",\n'
            '    "form_field_key_2": "값2"\n'
            "  }\n"
            "  // ❌ \"분류_근거\", \"판단_이유\" 등 추가 키 절대 금지!\n"
            "}\n"
            "- 🚨🚨🚨 절대 금지 사항:\n"
            "  * 임의의 키 생성 (예: \"CS_내용\", \"분류_결과\", \"분류_근거\", \"판단_이유\", \"검토의견\" 등)\n"
            "  * text 값을 키로 사용 (예: \"문의 유형\", \"검토 결과\" 등 한글 키)\n"
            "  * 근거나 이유를 별도 키로 분리 (모든 근거/이유는 \"수행한_작업\"에 포함)\n"
            "  * 최상위 키는 오직 \"상태\", \"수행한_작업\", \"폼_데이터\" 3개만 허용\n"
            "  * 백틱/코드블록으로 감싸기 (순수 JSON 객체만 반환)\n"
            f"{report_slide_notice}"
        )

    def _build_system_prompt_description(self) -> str:
        """description 전용 시스템 프롬프트 생성"""
        return (
            "당신은 CrewAI Task description을 작성하는 전문가입니다.\n\n"
            "역할: 주어진 컨텍스트 정보를 바탕으로 에이전트가 수행할 구체적인 작업 지시(description)를 생성합니다.\n"
            "핵심 원칙: 도구 실패/파일 없음 등은 작업 실패 사유가 아니며, 입력 컨텍스트만 있으면 작업 완료 가능하도록 지시를 작성합니다.\n"
            "응답 형식: 설명 텍스트 한 문단으로만 응답하세요. 백틱/코드블록/JSON 포장 금지."
        )

    def _build_system_prompt_expected_output(self) -> str:
        """expected_output 전용 시스템 프롬프트 생성"""
        return (
            "당신은 CrewAI Task expected_output을 작성하는 전문가입니다.\n\n"
            "역할: 주어진 폼 정보(form_types/form_html)만을 바탕으로 결과 형식(expected_output)을 생성합니다.\n"
            "핵심 원칙: FAILED는 오직 입력 컨텍스트가 전혀 없을 때만 사용하며, 도구 실패/파일 없음 등은 실패 사유가 아닙니다.\n"
            "응답 형식: 오직 JSON 객체로만 응답하세요. 백틱/코드블록/문자열 포장 금지."
        )
    
    def _collect_dmn_analysis(
        self,
        agent_info: List[Dict],
        task_instructions: str,
    ) -> Dict[str, str]:
        """에이전트별 DMN 규칙 분석 결과 수집: task_instructions를 dmn_rule 쿼리로 사용"""
        if not task_instructions or not task_instructions.strip():
            return {}

        dmn_results: Dict[str, str] = {}
        for ag in agent_info:
            agent_id = ag.get("id") or ag.get("user_id")
            tenant_id = ag.get("tenant_id")
            role = ag.get("role", "Unknown")
            if not (agent_id and tenant_id):
                continue
            try:
                tool = DMNRuleTool(tenant_id=tenant_id, user_id=agent_id)
                result = tool._run(task_instructions.strip())
                if result and isinstance(result, str):
                    dmn_results[role] = result
            except Exception as e:
                logger.warning("⚠️ 에이전트 %s DMN 규칙 분석 실패: %s", role, e)

        return dmn_results